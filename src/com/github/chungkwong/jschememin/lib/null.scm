(define-library (scheme null)
  (import (scheme base))
  (export and begin case cond define define-syntax delay do if lambda let let* let-syntax letrec letrec-syntax
          or quasiquote quote set!))